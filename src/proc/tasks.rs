struct Registers {
    edi: u32,
    esi: u32,
    ebp: u32,
    ebx: u32,
    edx: u32,
    ecx: u32,
    eax: u32,

    ip: u32,
    cs: u32,
    flags: u32,
    esp: u32,
    ss: u32,
}

struct Task {}
